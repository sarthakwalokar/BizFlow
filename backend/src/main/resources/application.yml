server:
  port: ${PORT:8080}
  error:
    include-message: always
    include-binding-errors: always

spring:
  application:
    name: bizflow-backend
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/bizflow_db}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      idle-timeout: 30000
      connection-timeout: 20000
      max-lifetime: 1800000

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
        jdbc:
          lob:
            non_contextual_creation: true

  flyway:
    enabled: true
    baseline-on-migrate: true
    baseline-version: 0
    locations: classpath:db/migration

# Application Custom Properties
app:
  jwt:
    secret: ${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
    expiration-ms: ${JWT_EXPIRATION_MS:86400000} # 24 hours
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:3000,http://127.0.0.1:5173,https://bizflow-frontend-spa8.onrender.com}
    allowed-methods: GET,POST,PUT,PATCH,DELETE,OPTIONS
    allowed-headers: "*"
    allow-credentials: true
    max-age: 3600

# OpenAPI / Swagger
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: method
    tagsSorter: alpha
    tryItOutEnabled: true

# Actuator Endpoints
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

# BizFlow AI Business Assistant Configuration
bizflow:
  ai:
    enabled: ${BIZFLOW_AI_ENABLED:true}
    gemini:
      api-key: ${BIZFLOW_AI_GEMINI_API_KEY:}
      model: ${BIZFLOW_AI_GEMINI_MODEL:gemini-1.5-flash}
      base-url: ${BIZFLOW_AI_GEMINI_BASE_URL:https://generativelanguage.googleapis.com/v1beta}
      timeout-ms: ${BIZFLOW_AI_GEMINI_TIMEOUT_MS:15000}
    groq:
      api-key: ${BIZFLOW_AI_GROQ_API_KEY:}
      model: ${BIZFLOW_AI_GROQ_MODEL:llama-3.3-70b-versatile}
      base-url: ${BIZFLOW_AI_GROQ_BASE_URL:https://api.groq.com/openai/v1}
      timeout-ms: ${BIZFLOW_AI_GROQ_TIMEOUT_MS:15000}
    openrouter:
      api-key: ${BIZFLOW_AI_OPENROUTER_API_KEY:}
      model: ${BIZFLOW_AI_OPENROUTER_MODEL:meta-llama/llama-3.3-70b-instruct}
      base-url: ${BIZFLOW_AI_OPENROUTER_BASE_URL:https://openrouter.ai/api/v1}
      timeout-ms: ${BIZFLOW_AI_OPENROUTER_TIMEOUT_MS:15000}

---
# Local In-Memory Profile (Instant Run without PostgreSQL setup)
spring:
  config:
    activate:
      on-profile: local
  datasource:
    url: jdbc:h2:mem:bizflow_db;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password: ""
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
  flyway:
    enabled: false

